# omarcat — 討論與決策紀錄

一個給 Omarchy bar 的系統監視器,bar 上是一隻隨負載奔跑的貓,點開是
iStat Menus 風格的面板。

## 出身

**基礎是 `crmne.omastats`(Carmine Paolino,MIT)的 fork。** 那個專案已經有
一套完整的取樣器與面板框架,重寫一遍沒有意義。

> **2026-09-12:這個專案決定不發行。** 純自用。
>
> 這改變了後面好幾件事的判斷標準:跑者可以用任何來源的素材(見「跑者系統」那節)、
> README 不必重寫、也不需要送市集審查。**但 `LICENSE` 裡 crmne 的版權行仍然要留** ——
> MIT 的條件不因為你不發行而消失,而且哪天改變主意時不用回頭補。

原本為了發行而列的義務,保留在這裡當紀錄:

- `LICENSE` 保留 crmne 的版權行,再加上自己的
- README 開頭註明「based on OmaStats by Carmine Paolino」
- `ui/frames/*.svg`(4 個胖瘦 × 5 個跑步畫格)是 crmne 的原創美術,不是移植自
  Kyome 的 macOS RunCat。沿用要一併註明。

「RunCat」這個名字來自 Kyome 的 macOS app,概念(貓跟著 CPU 跑)也是他的。
專案名改用 `omarcat` 正好避開這件事。

## 已驗證的技術事實

### 純 QML 可以直接讀 /proc,不需要任何 binary

實測過(2026-09-11),不是推測:

| 檔案 | FileView 讀到 | 用途 |
|---|---|---|
| `/proc/stat` | 4843 bytes | 第一行算 CPU 使用率 |
| `/proc/meminfo` | 1671 bytes | `MemTotal` / `MemAvailable` 算記憶體 |

擔心「/proc 回報大小 0 會讀到空字串」是多慮的,`FileView` 讀得到完整內容。

數字也交叉驗證過:壓兩顆核心(共 8 核),spike 讀到 27–33%,同時段 `vmstat`
讀到 30–38%,差距來自取樣視窗未對齊和量測程序自身的開銷。**沒有數量級錯誤**
(這點特別確認過,先前在別處踩過 `CLK_TCK` 是 100 不是 1000 的坑)。

**坑:`FileView.reload()` 是非同步的。** 同一個 tick 裡呼叫 `text()` 會拿到空
字串 —— spike 的第一次取樣就是這樣失敗的。要在 `onLoaded` 裡處理,或永遠用上
一輪的結果。

意義:如果哪天想把某個功能拆成獨立、零相依的小 plugin,取樣這關不是障礙。
OmaStats 之所以要養 Rust 取樣器,是因為它還要 GPU、磁碟、網路、hwmon 感測器、
風扇、電池和行程排名。

### 取樣器已經提供 CPU 的細分

`sampler/src/cpu.rs` 已經算好並輸出 `user` / `system` / `iowait` / `busy`
四個欄位(第 278-280 行)。概覽頁要的 CPU 細分不必新增取樣。

### 面板加一個 tab 是小手術

- `Model.js:17` 的 `PANEL_TABS` 陣列決定順序
- `Model.pageFile(tab)` 把 tab id 對應到 `ui/` 下的檔名,由
  `OmaStatsWidget.qml:388` 動態載入
- 現成積木:`Card`、`CardHeader`、`BigStat`、`StatRow`、`MiniRing`、
  `RingGauge`、`HistoryGraph`、`Legend`、`SectionTitle`

## 開發方式

**一次討論一個 tab,談定了才做。**

## 里程碑 1:fork + 改名(2026-09-11,已驗證)

`crmne.omastats` → `io.github.andyweiboan.omarcat`,名稱 `Omarcat`,版本 0.1.0。

**plugin id 被寫死在程式裡五個地方**,只改 `manifest.json` 不夠 —— 面板會顯示
「service is not loaded」而且所有數值是 0:

- `OmaStatsWidget.qml:13` `moduleName`
- `OmaStatsWidget.qml:16` `shell.serviceFor(...)`
- `OmaStatsWidget.qml:184` 警告訊息
- `OmaStatsService.qml:193-195` `shell.hide/toggle/summon`

另外 `OmaStatsService.qml` 的 `IpcHandler.target` 也是 `crmne.omastats`,不改的話
兩個 plugin 會搶同一個 IPC 名稱,shell 會警告「another handler is registered」。
已改成 `omarcat`。

驗證方式是直接問 IPC 拿數據,不是看截圖:

```
$ omarchy-shell omarcat status
{"ready":true,"cpu":3.6,"cpuTemp":68,"memoryPercent":21.3,"battery":62,...}
```

安裝方式是 symlink 到 `~/.config/omarchy/plugins/`,並在 `~/.config/omarchy/shell.json`
的 bar layout 裡把 widget id 換成新的(舊的 `crmne.omastats` 因此自動變 disabled)。
`shell.json` 備份在該次 session 的 scratchpad。

## Tab 1:概覽(Overview)

目標:打開面板第一眼就看到整台機器的狀態,不必切分頁。五個區塊:

1. CPU —— 百分比,加上 system / user / idle 的細分
2. 記憶體
3. 硬碟
4. 電池
5. 網路

其餘 tab 維持原樣。

### 最終形態:當下狀態,不碰時序

第一版做了走勢圖,推翻了。**概覽頁嚴格只呈現當下**:每個子系統一條進度條、
一個放大的數字、一行小字。時間軸留給各自的分頁 —— 兩者混在一起會讓這頁變成
「要讀的」而不是「瞄一眼的」。

進度條帶兩個資訊管道:**長度說有多滿,顏色說滿了要不要緊**。

新寫的兩個元件(原版只有圓環,沒有橫條):

- `ui/LevelBar.qml` —— 軌道 + 填充,依門檻換色,寬度與顏色都有過渡動畫。
  `inverted` 給電池用(低才危險),`forceNormal` 給充電中用。
- `ui/OverviewRow.qml` —— 標題 + 大數字 + 條 + 細節行的組合。

門檻:

| | 黃 | 紅 |
|---|---|---|
| CPU | ≥70% | ≥90% |
| 記憶體 | ≥70% | ≥85%(沿用原版 `MemoryPage.qml:49`) |
| 磁碟 | ≥80% | ≥90% |
| 電池(反向) | ≤30% | ≤15%,充電中一律正常色 |

**網路不給進度條。** 進度條意味著「佔了容量的多少」,而網路沒有可除的容量 ——
100 KB/s 是快是慢取決於線路。用「本次開機的最大值」當分母的話,分母會一直變,
條就在說謊。

### 已驗證 / 未驗證

實測壓 CPU,三段顏色都確認會變:

| CPU | 條 |
|---|---|
| 3% | 藍(正常) |
| 82% | 黃(警告) |
| 100% | 紅(危險) |

**未驗證:電池的反向門檻與充電中強制正常色。** 要把電池放到 30% 以下才測得到,
沒有實測就不宣稱它對。

### 過程中踩到的坑

`history.memUsed` 存的是**百分比**(`OmaStatsService.qml:78`),不是位元組。
第一版把圖的 ceiling 設成 `mem.total`(31GB),整條線被壓扁成看不見。
走勢圖後來整個拿掉了,但這個單位陷阱對其他頁仍然有效。

### 早期設計決策(仍然成立)

1. **CPU 不顯示閒置。** 閒置 = 100 減其他項,是推導值,佔一行卻不帶新資訊。
   改顯示 I/O wait —— 那才是「卡在磁碟」的真訊號。
   取樣器已經有 `user` / `system` / `iowait` / `busy`(`sampler/src/cpu.rs:278`),
   不需要新增取樣。
2. **硬碟只顯示容量**,不顯示讀寫速度。I/O 活動留給原本的「硬碟」分頁。
3. **多顆硬碟時只看根目錄所在的那顆。** 其餘留給「硬碟」分頁。
4. 沒有電池的機器自動隱藏電池那列(框架本來就有 `hasBattery` 判斷)。

## Tab 2:Processor(原 CPU)

### 決策

1. **分頁名稱由 CPU 改為 Processor** —— 同一頁也承載 GPU,叫 CPU 不準確。
   只改 `MODULES` 裡的 `label`,**`id` 維持 `"cpu"`**:那個 id 會原樣出現在使用者的
   設定字串(`modules`、`tabs`)和 manifest 的 aliases 裡,改掉會默默弄壞既有設定。
2. **走勢圖保留,右邊同一排加溫度圓環**(`RingGauge`,元件本來就有)。
   左圖是過去幾分鐘,右環是此刻。溫度不畫進走勢圖,因為它的尺度不同,得加第二條軸。
3. **核心列出實體核心,不是執行緒**,每顆兩條 bar:Load 與 Temp。

### 為什麼是實體核心

溫度感測器屬於**核心**,兩個超執行緒共用一個讀數。若照執行緒列,同一個溫度會被
印兩次,暗示了硬體並不具備的精度。Load 取該核心兩個執行緒的平均 —— 兩個超執行緒
共用同一組執行資源,「這顆半忙」比「取最大值」誠實。

### 坑:取樣器沒有輸出執行緒與核心的對應

`cpu.cores` 是長度等於**執行緒數**的扁平陣列,另外只給 `coreCount`。兩者之間的
對應**沒有**被輸出,而且不能假設:

- 這台 i7-1185G7 是 `cpu0..3 → core 0..3`、`cpu4..7 → core 0..3`(即 `i` 與 `i+coreCount` 成對)
- 其他 CPU 是相鄰配對(`0,1` 一顆、`2,3` 一顆)

任一種假設都會在另一半機器上算錯。解法是新寫的 `ui/CpuTopology.qml`,直接讀
`/sys/devices/system/cpu/cpuN/topology/core_id`(拓樸不會變,開機讀一次)。
**不需要重編 Rust 取樣器** —— 這正是先前 spike 證明過的那條路。

### 溫度來源與刻度

- 每核心:`sensors.temps` 裡 label 形如 `Core N` 的項目(Intel `coretemp` 提供)。
  AMD 的 `k10temp` 通常只給一個封裝溫度,那時 `coreTemps` 是空的,**溫度那條 bar
  會自己消失**,而不是編一個數字出來。
- 圓環:label 形如 `Package id N` 的項目,沒有就退回 `cpu.temp`。
- 刻度用感測器自己回報的 `max`/`crit`(這台是 100°C),沒有才退回 100。
  這樣條的意思是「離降頻還有多遠」,而不是「離一個沒有物理意義的整數還有多遠」。

溫度不是呼叫 `sensors` 指令或解析它的輸出,是取樣器直接走 `/sys/class/hwmon/*`
讀 `tempN_input`(毫度,要除以 1000),用驅動名稱分類(`coretemp` / `k10temp` 都算 CPU)。

### 版面:反覆調整後的定案

分頁最後的組成:**CPU 卡片 → 四顆實體核心 →(GPU,有讀數才出現)→ 行程列表**。

```
CPU  up 4h 32m
┌────────────────────────────────┬──────────────┐
│ 走勢圖(User / System 堆疊)     │   ◜ CPU ◝    │
│                          2/3   │   5%  61°    │
│                                │   856 MHz    │
└────────────────────────────────┴──────────────┘
```

走到這裡經過好幾輪,把**試過又推翻的**也記下來,免得以後有人再走一遍:

| 改動 | 結果 |
|---|---|
| 頂部加 CPU/GPU 型號規格區塊 | **推翻**,拿掉。資訊是靜態的,佔掉第一眼的位置不值得 |
| 圖與環寬度嚴格 2/3 : 1/3 | 保留欄寬比例,但**環不能填滿它那一欄** —— 環是正方形,填滿就等於把整條帶子的高度撐到 1/3 寬,左邊的走勢圖多出一大塊死空間。改成設尺寸上限、在欄內靠右 |
| 圓環弧線畫溫度 | **推翻**,改畫使用率。原本弧線畫溫度、中間數字也是溫度,後來數字改成使用率,弧線和數字就變成兩件事;現在一致 |
| 卡片標題用 `inlineDetail` 把頻率溫度移到 `CPU` 旁邊 | 做了,**後來整個拿掉**。兩個數字都進了圓環之後,標題那行變成一字不差的重複。`CardHeader.inlineDetail` 這個屬性留著沒刪,其他頁可能用得上 |
| 圖例 `User / System` 與 `Cores / Threads` | 兩處都移除 |
| Load average 補上分母、三條 bar | 做完之後**整張卡片移除** —— 見下 |

環的尺寸有上下限:上限避免撐高整排,**下限是必要的** —— 太小的話底下那行頻率會被切成 `1.07 G.` 並且壓到弧線上。

### Uptime 與 Load average:一個搬家、一個刪掉

原本兩者共用一張卡片,左右各半。這是錯的配對:**uptime 是靜態事實(開機多久),
load average 是即時讀數(現在多忙)**,並排會讓人以為是同一類東西。

- **Uptime 搬到 CPU 卡片標題旁**(`CPU  up 4h 32m`)。那行在頻率溫度都進圓環之後
  是空的,正好給它。
- **Load average 整張卡片移除。**

移除前先把它做對過一輪,結論仍然值得記下來,因為問題本身是真的:

> 三個裸數字無法解讀,因為**沒有分母**。`0.52` 在這台 8 執行緒機器上幾乎全閒,
> 在單核機器上是半滿,而畫面上沒有任何東西說明是哪一種;也沒有標示那三個數字
> 分別是 1、5、15 分鐘的平均。
>
> 做法是:標出週期、右上角寫出分母、條長 = `load ÷ 執行緒數`,滿格即「每條執行緒
> 都有事做」,滿格轉黃、1.5 倍轉紅。**分母要用執行緒數而不是實體核心數** ——
> load average 數的是可執行的工作數,能同時跑幾個由執行緒決定。

之後判斷這個資訊對日常沒有重要到值得佔一張卡片,整張拿掉。要復原的話,上面
這段就是規格。

**連帶清理:`showLoad` 這個設定開關。** 它原本控制那張卡片,卡片沒了就成了孤兒
——設定頁還會列出「Load average and uptime」,按了卻什麼都不會發生。已從
`Model.js` 的 `SETTINGS` 與設定項清單移除。`Model.loadText()` 則保留未刪,那是
原專案的公用函式,不是我們加的。

### 走勢圖的圖例要和圖同一欄

圖例第一版掛在整個 `Row` **下面**,寬度是卡片寬 —— 結果兩個問題:數值飄到圓環底下
去了(圓環佔右邊 1/3,圖只有 2/3),而且 `Row` 的高度由較高的圓環決定,圖的基線和
它自己的標籤中間硬生生被撐開一段死空間。

改成左欄是「走勢圖 + 兩行圖例」一整組、右欄是圓環垂直置中,兩欄高度相當,空隙消失,
卡片矮了約 100px。走勢圖高度設成圓環的 0.62 倍,讓左欄總高度剛好和圓環相當。

圖例一行一組(`● User ... 4%` / `● System ... 2%`),不是一行塞兩組 —— 後者會讓
第二個數值被推到最右緣變成孤兒,那正是先前被要求拿掉的東西。

### 圓環要在大數字旁邊再放一個讀數:`trailingText`

`RingGauge` 原本只能上中下三行(`topText` / 數字 / `subText`)。要把溫度放在使用率**右邊**而不是下面,新增了 `trailingText`,並把原本單獨置中的 `Measure` 包進一個置中的 `Row` —— 否則加上溫度會把大數字擠歪,而不是並排。屬性預設空字串,`MemoryPage` 的環不受影響。

**對齊的陷阱:`Measure` 是普通 `Item`,它的 `baseline` 錨線在頂端**,不在數字的基線上。寫 `anchors.baseline: figure.baseline` 會把小字貼到大數字的上緣。改成對齊底部 —— 數字沒有下伸部,底部對齊實際上就落在基線。

### GPU 卡片:上游的 `isFinite(Number(null))` 陷阱

**`Number(null)` 是 `0`,而 `isFinite(0)` 是 `true`。** 所以這個看起來很合理的判斷

```qml
isFinite(Number(root.gpu.util)) ? String(Math.round(root.gpu.util)) : "—"
```

會讓 `null` 走進「有值」那一條,把「沒有這個讀數」印成 **`0%`**。

這是**原專案(OmaStats)的 bug,不是 fork 改出來的**,而且影響所有 Intel 內顯的
使用者 —— 值得回報上游。同一張卡片的 Power 那行有多補一道 `!== null`,所以正確地
隱藏了;util 那行漏了。

修法是一個共用判斷:

```qml
function hasNumber(value) {
  return value !== null && value !== undefined && isFinite(Number(value));
}
```

`null` 和 `0` 是兩個不同的答案,只有一個是量測結果。

**Intel 內顯實際拿得到什麼**(這台 Iris Xe):只有 `name`、`vendor`、`mhz`、
`maxMhz`。`util` / `temp` / `power` / `memTotal` **全部是 null** —— 使用率藏在
i915 perf 後面,需要 shell 沒有的權限。AMD 和 NVIDIA 才從 sysfs 給得出來。

所以卡片的顯示條件改成「GPU 至少答得出一個即時讀數」(util、temp、power、VRAM
任一)。**沒有讀數就整張隱藏** —— 一張只有時脈和四個破折號的卡片比沒有更糟。
同構的 Intel 筆電上它消失,有獨顯的機器上照常出現。

### 待辦:混合架構的核心類型沒地方顯示

移除的 `Cores / Threads` 圖例,在 Intel 的 P-core / E-core 機器上原本會改成顯示
「Performance 幾顆 / Efficiency 幾顆」。拿掉之後那類機器看不到核心屬於哪一種。
取樣器有 `cpu.efficiency`(E-core 的索引陣列),這台是同構四核所以是空的。
要補的話,**位置應該在每一列的 `Core N` 標籤旁邊加個小標記**,而不是恢復那個圖例。

### 驗證上的坑

- `omarchy-shell shell call <id> showTab cpu` 對 **bar-widget 類的外掛達不到**,
  回傳 `unknown`。要截某個分頁的圖,只能改預設讓它開在那頁,截完再改回來。
- 面板的 tab 選取狀態與滑鼠 hover 的底色**長得幾乎一樣**,截圖時游標若停在
  tab 列上會誤判。先 `hl.dsp.move_cursor` 把游標移開再截。

## Tab 3:Memory

### 主數字用 `available` 算,不用 Linux 的 `used`

Linux 的 `used` **不含可回收的快取**,所以它讀起來遠低於實際被佔用的量,會讓人在快沒
記憶體的機器上以為「還很空」。`available` 是核心自己對「新的配置拿得到多少」的估計,
所以主數字是 `(total − available) / total`。

### 一條堆疊條,不是四條獨立的條

四個分量**精確加總等於 total**,對著取樣器驗證過,一個位元組不差:

```
apps 2.69 + cached 2.88 + shared 2.22 + free 23.26 = 31.05 GB = total
```

所以堆疊是事實而不是近似。四條獨立的條會藏起「它們在瓜分同一塊」這件事。
新元件 `ui/StackBar.qml` 就是為此而生。

### Swap 獨立一張卡片

它是**不同的資源、不同的總量**,混進上面的堆疊條會暗示 RAM 和 swap 可以相加。
門檻訂得很低(10% 黃、50% 紅):swap 不是拿來填滿的容量,是你寧可不要碰到的退路。

### 壓力(PSI)只在大於 0 時才顯示

`pressureSome` / `pressureFull` 是核心直接回報「有多少時間卡在等記憶體」,**比百分比準**
—— 用到 90% 但壓力 0 是沒事的,用到 60% 但壓力 20% 是有事的。但它平常永遠是 0,
**一個永遠是 0 的數字會訓練人忽略它**,所以平常不顯示,附在圖例尾端只在有值時出現。

### 版面演進(又是試了才知道)

第一版是「走勢圖 2/3 + 圓環 1/3」,跟 Processor 一致。後來:
環與圖左右對調 → 再改成拿掉環、用 progress bar 放在圖上方 → 最後發現那條單色 bar
和下面的三色堆疊條說的是同一件事,**合併成一張卡片**:標題 + 右側大數字 + 堆疊條 +
圖例 + 走勢圖。

`showBreakdown` 設定因此成為孤兒(原本控制那張獨立的分項卡片),已移除 ——
跟 `showLoad` 同樣的處理。

## Tab 4:Disks

### 容量按掛載點、溫度與讀寫按實體磁碟

跟 CPU 那邊「使用率按執行緒、溫度按實體核心」是**同一個不對稱**。`/` 和 `/boot` 在同
一顆 NVMe 上,把 39° 印在兩個掛載點旁邊是假的精度。溫度只在活動那張卡片出現一次,
標題是實體磁碟名(`nvme0n1`)。

### 掛載點合併成一條,但條的語意換了兩次

一開始每個掛載點各一條 —— 理由是「兩個獨立的總量不能相加」。**這個理由是錯的**:
放回實體磁碟上看,它們確實在瓜分同一塊 477 GB。改成一條。

但第一版的分段是**按已用量**,結果 `/boot` 佔總量 0.0135%,換算 **0.06 個像素**,
完全看不見(使用者發現的)。

最終版分段改成**按容量**、分段內填滿的部分表示已用 —— 也就是 GParted 那種分割區
配置圖。`/boot` 有 2 GB,佔 0.4%,細但存在。

### `LevelBar` 與 `StackBar` 的最小寬度規則是相反的,這是刻意的

| | 有沒有最小寬度 | 為什麼 |
|---|---|---|
| `LevelBar` | **有** | 它量的是「多滿」。CPU 0.4% 也該看得見 —— 「幾乎沒有」和「完全沒有」是不同的狀態 |
| `StackBar` 的分段 | **沒有** | 它量的是**比例**。給小分段最小寬度就等於謊報 `/boot` 佔磁碟 1% |
| `StackBar` 的 fill | **有** | 那是「有還是沒有」,放了東西的分割區不該看起來是空的 |

### 每一列要帶自己的百分比

條上面的 8% 是整顆磁碟的,但 `/boot` 佔整顆只有 0.4%,**一個 90% 滿的 `/boot` 在條上
完全看不見,而開機會失敗**。所以每列額外寫 `3% of 2.0 GB` —— 條回答「怎麼瓜分」,
列回答「這個分割區快不快滿」。

## 「Where it went」:為什麼不做 macOS 式的分類餅圖

Andy 問能不能像蘋果那樣列出各類型的大小。查證後的結論是**不能,而且硬做會是錯的**。

macOS 的分類(應用程式、文件、照片、系統資料)來自 Spotlight 索引和系統 API,
Linux 沒有等價物。更關鍵的是這台機器上的數字:

| 來源 | 大小 | 成本 |
|---|---|---|
| pacman 資料庫(套件) | 8.8 GB / 991 個 | **0.087 秒**,不掃檔案系統 |
| `du -xd1 ~` | 3.3 GB | 0.2 秒 |
| `du -xd1 /` | 10.0 GB | 1.6 秒 |
| **可走訪路徑合計** | **~13.4 GB** | |
| **btrfs 實際回報 used** | **39.88 GiB** | |

**差 27 GB,佔總用量三分之二。** 原因查到了:`/.snapshots` 存在(snapper),而
`btrfs subvolume list` 和 `qgroup show` 都回 `Operation not permitted`。快照持有的資料
不在任何活著的路徑底下,非特權程式看不到。

所以分類餅圖會是「套件 8.8、家目錄 3.3、**其他 27**」—— 68% 落在「其他」。
**那看起來像個答案但不是。**

### 改做的東西

`bin/omarcat-storage`(POSIX sh)+ `ui/StorageScan.qml`,原則:

1. **誠實** —— 快照那格明寫 `needs root`,`Unaccounted` 那列註明 `mostly snapshots`,
   而不是把它折進去假裝知道
2. **可行動** —— pacman 快取和 `~/.cache` 標 `clearable`,那是真的刪了就回來的
3. **按需** —— 明確的 Scan 按鈕,不是每秒取樣。這台 0.18 秒,但冷快取、機械硬碟或
   塞滿小檔的家目錄會慢很多
4. **失敗就不發布** —— 腳本 exit code 非 0 時不更新畫面,避免顯示一份加不起來的分類

## 未決:行程列表要不要留

Andy 覺得「感覺沒啥用」。我的看法是**不要拿掉** —— 它是整個外掛裡唯一回答「是誰」的
東西,其他全部只說「有事情正在發生」。但他的直覺有對的部分:

| 分頁 | 閒置時 | 有負載時 |
|---|---|---|
| Memory | 有用 | 有用 |
| Processor | 噪音 | 有用 |
| Disks | **沒用**(永遠 `No disk activity`) | 有用 |
| Network | 多半沒用 | 有用 |

提案(尚未實作):**列表為空或全部趨近於零時整張卡片隱藏**。`showProcesses` 開關
本來就在,要完全關掉隨時可以。

## 刪掉 Sensors 分頁,風扇搬到 Overview

那張分頁上有兩類東西,**溫度已經各自有家了** —— CPU 的在 Processor(圓環 + 每核心
的 Temp 條),NVMe 的在 Disks。剩下三個孤兒溫度(ACPI、ThinkPad CPU、Wi-Fi)其中
兩個還跟 coretemp 的封裝溫度重複。

**但風扇轉速沒有第二個地方顯示**,而它是唯一「你聽得到」的讀數 —— 這台 CPU 只有 4%
卻兩顆風扇都在 5400+ rpm,那本身就是訊號。所以分頁刪掉,風扇移到 Overview。

### 風扇那列不給進度條

風扇沒有「容量」可以當分母:最高轉速因機型而異、沒有文件,拿「本次開機看過的最快」
當分母的話分母會在讀者眼前移動。跟網路那列同一個理由。

顯示**最快的那顆**而不是平均:問題是「這台機器為了維持溫度多拼命」,一顆全轉就回答
了。沒有風扇感測器的機器整張卡片隱藏,**不顯示 `0 rpm`** —— 那看起來像風扇停了,
而不是沒有感測器。

### 連帶清理

`SensorsPage.qml`、`MODULES` 的 sensors 項、`PANEL_TABS`、`SETTINGS.tabs`、
`sensorsStyle`、`barSensors`、`showTemperatures`、`showFans`、manifest 的 aliases /
defaults / 說明、以及 `parseModules` 裡 `temp`/`temps`/`sensor` 的別名。

`ui/BarReadout.qml` 的 `case "sensors"` 成為到不了的死碼(`parseModules` 不再接受
這個 id)。**刻意留著沒動** —— 那是共用元件,改它的風險大於收益。

## Overview 的溫度:有感測器才顯示

| 項目 | 溫度來源 |
|---|---|
| CPU | `sensors.temps` 裡 `Package id N` —— 整顆晶片,也就是會觸發降頻的那個 |
| 磁碟 | 該卷所在實體磁碟的 `temp`(`/` 和 `/boot` 會相同,但這裡只顯示系統所在的卷) |
| 網路 | `sensors.temps` 裡 chip 為 `Wi-Fi` 的項目 |
| **記憶體** | **取樣器沒有這個欄位** —— DIMM 的 SPD 溫度只有部分桌機暴露 |
| **電池** | **取樣器沒有這個欄位** |
| GPU | `null`(Intel 內顯什麼都不給,見前面那節) |

**沒有讀數的項目不留空位**,而不是顯示 `—`:一個空的溫度欄看起來像壞掉,而「這個
硬體沒有這顆感測器」是完全正常的狀態。

溫度一律放在該列細節的**最後**,因為它跟前面那些是不同種類的量 —— CPU 那列前三項是
百分比的拆分(加起來有意義),溫度是獨立的物理量。

## Tab 5:Network

### 訊號強度**可以**給進度條(跟風扇相反)

dBm 有公認的刻度:約 -30 是極限,-67 是串流的底線,-80 以下不堪用。所以把
**-90…-30 映射成 0…100%**,條的意思是「還剩多少可用訊號」。門檻**反向**(低才糟):
-67 轉黃、-80 轉紅。

這正好和風扇那列形成對照:風扇沒有分母,訊號有。**判斷標準不是「這是不是一個數字」,
而是「它有沒有一個有意義的滿格」。**

### 最有行動價值的一行:頻段 + 頻道 + 速率

單看 `87 Mbps` 不知道是差還是正常。配上 `2.4 GHz · ch 11` 就一目了然 ——
**這台訊號 -40 dBm(極佳)卻只跑 87 Mbps,因為連在 2.4 GHz。** 換 5 GHz 會快好幾倍。
一頁系統監視器能講出這種話,比多畫三張圖有用。

頻道用標準編號換算(2.4G / 5G / 6G 三段各自的公式),這樣數字和路由器設定頁上寫的
一致。

### `publicIp` 預設改成關閉

原專案預設**開啟**,面板一打開就去問 `api.ipify.org`。也就是說這個外掛會**主動連線到
第三方,把使用者的公開 IP 告訴對方**。

自己用沒差,但這是要發行到市集的東西 —— 而且 Mission Control 才剛因為視窗標題被安全
審查擋過一次。已改成預設 `false`,`Model.js` 和 manifest 都改,說明文字寫明這是對第三
方的外送請求。想看的人自己開。

### 其他卡片

- **活動**:標題是介面名 + Wi-Fi 溫度;鏡像圖(上傳在上、下載在下)沿用原專案的 `MirrorGraph`
- **位址**:IPv4 / IPv6,點一下複製(原專案的行為,保留)
- **自開機累計**:`rxTotal` / `txTotal`。速率不會告訴你用掉多少額度,這個會 —— 對計量型網路(手機熱點)才有意義
- **其他介面**:沒在用的縮成一行小字。一張沒連線的 USB 網卡不該和正在承載流量的連線平起平坐

### 已知未修

IPv6 那列的標籤被位址擠成 `IP…`。`StatRow` 是「標籤靠左、數值靠右」,而完整的 IPv6
位址幾乎佔滿整列寬。需要中間省略或換行,還沒處理。

## 附錄:風扇狂轉的實地調查(2026-09-11)

會記在這裡,是因為它**驗證了把風扇放進 Overview 的理由** —— 那是唯一「你聽得到」的
讀數,而這次它確實指向一個真實的狀況。

量到的:

| | |
|---|---|
| 風扇 | 7481–7495 rpm,**幾乎完全不動** |
| CPU 溫度 | 55–70°C 之間跳動 |
| 所有熱區 | ≤ 61°C,沒有一處燙 |
| 風扇控制 | `/proc/acpi/ibm/fan` → `level: auto`,不是被鎖住 |
| CPU 負載 | 3–8%,無失控行程;中斷與 context switch 正常 |
| **電池** | **充電中,灌入 35.7–37.9 W**,充電上限 100% |

**關鍵觀察:風扇轉速與 CPU 溫度完全不相關。** CPU 在 15 度的範圍內來回擺,風扇卻釘在
7485 上下。曲線若由 CPU 帶動,轉速會跟著起伏 —— 釘死的高速代表 EC 在回應另一個**持續**
發熱的東西。

**判斷:熱源是充電電路。** 36 W 灌進電池加上系統本身的消耗,薄機殼要散掉 45–55 W。
「不燙卻狂轉」正是這個狀態 —— 它不燙,是因為風扇在全力壓著。

**後續觀察(同日稍晚):**電池從 21% 充到 32%,風扇**一路釘在 7490 rpm 沒下來過**,
充電功率仍在 35 W 左右。與「充電發熱」的判斷一致 —— 要等充到 80% 以上快充退場才會看到
風扇降。

**尚未排除:**散熱膏老化或進氣口積灰(症狀相同:溫度正常但風扇很拼)。
**分辨方法:拔掉充電器。** 風扇明顯下降就是充電發熱;仍然很高就要查散熱通道。
(此測試尚未執行。)

技術註記:`intel_idle` 在這台採用 BIOS 的 `_CST` 表,最深只到 `C3_ACPI`,沒有 11 代
常見的 C8/C10。我一度誤判成「驅動掉回 ACPI 通用版」,實際驅動就是 `intel_idle`。
RAPL(`intel-rapl:*/energy_uj`)需要 root,所以沒能直接量到封裝功耗。

## 刪掉 Battery 分頁,health 搬到 Overview

分頁上的東西幾乎都是重複的:百分比和充電狀態 Overview 有、bar 上的 `omarchy.power`
也有;電壓、型號、電源供應器那些平常不會看。

**只有兩個數字在別處看不到:`health`(90.7%)和 `cycles`(107)。**

- **health 搬到 Overview 的電池那列** —— 它是「一年看一次」的數字,不值得一張分頁,
  但值得在你剛好看電池時順眼掃到
- **cycles 不搬** —— 它只有配合 health 才有意義,而 health 已經把結論講完了
  (107 次循環對應 90.7% 是正常衰退)

### 唯一真正失去的功能

`battery.peripherals` —— 有藍牙滑鼠、鍵盤或耳機時會列出它們的電量。這台是空陣列所以
沒有影響,但**在有周邊的機器上這是真的損失**。Omarchy 自己的藍牙 widget 可能涵蓋。
要復原的話,那個欄位還在取樣器的輸出裡。

### 連帶清理

`BatteryPage.qml`、`MODULES` 的 battery 項(**連 bar 上的電池讀數一起移除** ——
`omarchy.power` 已經在做這件事)、`PANEL_TABS`、`SETTINGS.tabs`、`batteryStyle`、
`showHistory` / `showDetails` / `showDevices`、設定頁的 battery 區塊、manifest 的
aliases 與說明、`parseModules` 的 `bat` 別名。

`Model.panelTabs(hasBattery, tabsSetting)` 的第一個參數成為無用參數。**保留簽名並加
註解**,因為呼叫端在 `OmaStatsWidget.qml`,改它沒有好處。

## 分頁刪減的總帳

| | 原 OmaStats | omarcat |
|---|---|---|
| 分頁 | cpu, memory, disks, network, sensors, battery | **overview**, processor, memory, disks, network |
| 新增 | — | Overview(全新) |
| 刪除 | — | Sensors(風扇搬 Overview)、Battery(health 搬 Overview) |
| 改名 | CPU | **Processor**(同頁也承載 GPU) |

七個分頁縮到六個(含 Settings)。判斷的共同標準是:**這張分頁有沒有回答一個別處回答
不了的問題。** 有就留,沒有就把那唯一的答案搬到 Overview,然後刪掉。

## 設定頁大砍:559 行 → 281 行

### 前提:bar 上只剩貓

原專案在 bar 畫一排可設定的 CPU/MEM/NET 讀數,貓只是其中一個選項(`modules: "cat"`)。
**omarcat 只留貓。** 市集上已經有兩千多個會畫數字的 bar 外掛,貓才是這個東西的識別;
而且「換 runner」這個未來功能建立在「bar 上就是一隻跑者」的前提上。

這一個決定就砍掉了:

- 六個設定:`modules`、`style`、`barLabels`、`graphWidth`,加七個 per-module 樣式覆寫
- 三個元件檔:`BarReadout.qml`、`StackLabel.qml`、`MiniRing.qml`

### 七個 `show*` 開關改成「沒內容就自己隱藏」

`showCores`、`showGpu`、`showVolumes`、`showActivity`、`showInterfaces`、`showTotals`、
`showAddresses` —— 這些控制「要不要顯示某張卡片」。**不是隱藏起來,是改成條件顯示**:
GPU 沒讀數就不出現、沒有其他網路介面就不出現。

**比叫人去設定頁決定好。** 一張沒有東西可說的卡片,不需要一個開關來決定它該不該在。

### 留下來的五項

| | 為什麼 |
|---|---|
| Temperature | 真實偏好(°C / °F) |
| Refresh every | 取樣頻率,直接影響耗電 |
| History | 走勢圖回溯多久 |
| Top processes | 一大塊版面,開關有意義 |
| Public IP | 隱私開關,預設關,底下附一句說明會告訴誰 |

`disksSource` **保留在程式裡但不放進 UI**:Disks 頁會自動認出單一實體磁碟,只有多硬碟
工作站需要指定,那種人可以直接改 `shell.json`。

Runner 的位置留著,等那個功能做的時候加進同一張卡片。

## 導覽:Overview 當首頁,其餘用鑽入

### tab 列就是面板太寬的原因

```qml
Math.ceil(tabs.spelledWidth) + Style.spacing.popupPadding * 2
```

面板的最小寬度是**六個分頁名稱橫排的寬度**,等於每一頁都被最長的那串名字撐開,
而不是被自己的內容決定。

拿掉之後改成 `Style.space(380)` —— 和 Omarchy 自己的 audio / bluetooth / power / monitor
面板同寬(它們都用 380)。465px → 380px。

### 新的導覽

- **首頁**:左上 `omarcat`,右上 `󰒓 Settings` 按鈕
- **每一列右邊一個 `›`**,點整列鑽進該子系統的詳細頁
- **詳細頁**:左上變成 `‹ Processor`,整個標題區都是返回的點擊區;齒輪按鈕每一頁都在

**只有真的有詳細頁的列才給 `›`。** Fans 和 Battery 沒有(分頁被刪了),Overview 上就是
它們的全部。**有箭頭卻點不動,比沒有箭頭更糟。**

整列都是點擊區,不是只有那個小箭頭 —— 一個字符寬的目標對誰都不友善。

### 設定入口是按鈕不是裸圖示

第一版是右上角一個齒輪圖示。改成有邊框的 `󰒓 Settings` 按鈕,因為 **tab 列拿掉之後它是
唯一進設定的入口,光一個圖示是在要人猜**。用的是 Omarchy 共用的 `Button`(它本來就
同時支援 `iconText` 和 `text`),所以邊框、hover、間距和面板其他按鈕一致。

### 字型陷阱:圖示要綁別名,不能綁解析後的字型

齒輪一開始用 emoji `⚙`,被字型堆疊渲染成**彩色**,和單色的面板格格不入。換成 Omarchy
自己用的 Nerd Font 字符 `󰒓`(`Model.MODULES` 裡 settings 那項本來就是它)。

**但這台機器的 monospace 是 Comic Code,它沒有這個字符** —— 實測 `fc-list ':charset=F0493'`
只列出 JetBrainsMono Nerd Font。之所以能顯示,是因為綁的是字型**別名** `monospace`,
fontconfig 會逐字符回退。

`Style.qml` 本身就註明了這件事:

> Bind `font.family` to `fontFamily` (so the alias path keeps working when the user
> runs `omarchy font set`), but read `resolvedFontFamily` when you want to *display*
> what's drawing.

**如果有人把它「優化」成綁 `resolvedFontFamily`,齒輪會變成豆腐方塊。** 已寫進註解。

### 未驗證

鑽入和返回**只有靜態截圖為證**,我沒有辦法在自動化裡點擊。實際的導覽行為需要人工確認。

## 跑者系統(2026-09-12)

bar 上那隻貓從「一組寫死的檔案」變成「可以換的跑者」。三個部分:載入機制、兩個
產生工具、五隻跑者。

### 載入:名字就是路徑

```qml
source: "frames/" + runner + "_f" + fatness + "_" + (frame + 1) + ".svg"
```

原本 `cat` 是寫死在字串裡的。每隻跑者都是同樣的 **4 個胖瘦 × 5 個畫格**,所以換一隻
就是換一個字串。不認得的名字退回 `cat`。

### 工具一:`tools/make-runner.py` —— 從規格生成

原本那 20 個檔不是畫出來的,是**一條公式**:

| 每格的組成 | 尾巴 path + 身體 rect + 頭 circle + 耳朵 2 個 path + 腿 4 條 path |
|---|---|
| 胖瘦 | 只改身體高度(11 → 19.1)與頭半徑(7 → 8.65),寬度固定 30 |
| 畫格 | 只改腿的端點 |

**產生器先重現了原本的貓,20 格逐一比對全對(`--check`),才拿來畫新的。** 沒有在
未驗證的工具上畫東西。

支援兩種骨架:

- **四足**(cat、dog):剛體身體 + 四條直線腿,胖瘦是公式
- **關節**(dancer):頭 + 軀幹 + 四肢各兩節,姿勢是**相位的函數**而不是手調

### 工具二:`tools/trace-runner.py` —— 從影像描圖

給無法用公式表達的跑者用。輸入是已經二值化的剪影點陣圖,輸出是 SVG 的矩形集合。

### 三個踩過的坑

**1. XML 註解裡不能有 `--`。** 我在產生的註解寫了 `make-runner.py -- edit the spec`,
librsvg 直接拒收整個檔案。

**2. 先縮再描,不是先描再縮。** 第一版在原始解析度描圖再縮小,每個來源像素變成
0.14 單位的矩形 —— 全部落在次像素,反鋸齒讓圖**整個呈灰色**,永遠達不到實心。
正解是先把點陣圖縮到目標尺寸,**一個像素對一個單位**。

**3. 相鄰矩形要重疊,否則有橫紋。** 兩個共用邊緣的矩形各自被反鋸齒成部分覆蓋,
兩半加起來不到一個實心像素,留下一條細縫,整隻動物看起來像被梳子梳過。

我第一個解法猜錯了:以為可以把「左右邊界相同的相鄰列」合併。實作後矩形數只從
**45 降到 41** —— 因為有機形狀幾乎每列寬度都不同,沒有東西可合併。

真正的解法是**每個矩形四周外擴 0.15 個來源像素**讓它們重疊,覆蓋率飽和,縫就消失。
外擴對稱所以剪影不偏移,0.15 相對於 30 像素高的圖是 0.5%,肉眼看不到。
(合併的機制留著,對規則形狀仍然有效。)

### 五隻跑者

| 名字 | 來源 | 備註 |
|---|---|---|
| `cat` | crmne 的原創美術(MIT) | 原本就有的那隻 |
| `catalpha` | RunCat 選單截圖 | **不可發行** —— Kyome 的素材 |
| `dog` | 自己寫的規格 | 垂耳 + 口鼻 + 翹尾 |
| `dancer` | 自己寫的相位函數 | 關節骨架,持續擺肩 |
| `sway` | 影片逐格描圖 | **不可發行** —— 來自第三方影片 |

`catalpha` 特別順利,因為**週期量得出來**:22 個畫格只有 5 種不同的外框簽章
(寬高 + 白點數),循環出現 A→B→C→D→E。所以不用取樣、不用內插,**直接拿原始的
5 格**,和載入器的結構天然吻合。

`sway` 則需要後製:量出每格肩線的實際偏移再**放大 2.2 倍**,而且只施加在髖部以上、
愈往上位移愈大。整個人一起平移的話圖示會在 bar 上滑來滑去;只動髖部以上才讀得出
是身體在站定的腳上傾斜。肩膀另外在 22%–46% 那一帶左右各膨脹 1 像素 —— 整體膨脹
只會變成一個胖子,限制在肩帶才讀得出是體格。

### 尺寸:用量的,不是用眼睛調的

```js
var RUNNER_SCALE = { cat: 1.10, catalpha: 1.05, dog: 1.06, dancer: 1.06, sway: 1.07 }
```

係數是 `32 ÷ 內容高度`,量自實際渲染的畫格 —— 意思是「把這隻的墨水放大到剛好
一個 bar 圖示高」,所有跑者因此等高。

**這組數字推翻了先前用眼睛調的版本。** 舊值是直立的 1.25、貓狗 0.85,理由是
「瘦高的需要更多高度才有份量」。量完發現**五隻填滿高度的程度差不到 5%** ——
真正差的是**寬度**(貓橫跨整個畫布,舞者只佔三分之一)。等於是用高度去補寬度不足,
結果讓人形比 bar 上所有圖示都高。

**不要把這組 1.05–1.10 「順手統一成 1.0」** —— 它們是各自量出來的。

## 效能:貴的是動畫,不是取樣

有人會直覺想調低取樣頻率來省電。量出來的分佈說明那是錯的旋鈕:

| | 佔一個核心 |
|---|---|
| shell(跑者重繪) | **8.5%** |
| 取樣器 | **0.5%** |

把畫格間隔從約 200ms 改成 2000ms,shell 的 CPU **從 8.5% 掉到量不到** —— 所以那
8.5% 幾乎全是動畫。閒置時跑者每秒重繪五次,每次要重算 SVG 再過一層 MultiEffect。

`refreshSeconds`(設定頁的 Refresh every,已存在)只管取樣器那 0.5%,從 1 秒調到
10 秒省下 0.45%,等於沒調。**要給開關的話應該是動畫速度。**(尚未實作。)

### 換算成瓦數:還沒成功

試過用電池讀數量,失敗了,原因值得記下來:

```
電池 energy_now 的最小步階 = 40 mWh
60 秒量測 → 能分辨的最小功率差 = 40mWh ÷ 60s = 2.4 W
```

要找的訊號約 0.5 W,**遠在解析度之下**。兩次量到相同數值不是「沒有差別」,是看不到。
加上量測期間 shell 一直在重啟,機器並不閒置(13.8 W vs 真正閒置的 7.2 W)。

要釘死瓦數需要**每組 5 分鐘、機器完全不碰**的量測。
